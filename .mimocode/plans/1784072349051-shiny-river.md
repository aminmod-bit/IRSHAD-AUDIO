# Plan: Rename Salaf Audio → IRSHAD + Logo + Demo

## Цель
Полностью заменить брендинг "Salaf Audio" на "IRSHAD" в проекте IRSHAD-AUDIO, создать SVG-логотип (favicon + сайдбар), и запустить демо-просмотр.

---

## Step 1: Замена текста "Salaf Audio" → "IRSHAD"

### Файлы для правки:

**`src/App.jsx`** (2 правки):
- Строка 110: `<div className="sb-logo-text">Salaf Audio</div>` → `IRSHAD`
- Строка 263: `<div className="pc-sub">Salaf Audio</div>` → `IRSHAD`

**`index.html`** (1 правка):
- Строка 12: `<title>Salaf Audio</title>` → `<title>IRSHAD</title>`

**`package.json`** (1 правка):
- Строка 2: `"name": "salaf-audio"` → `"name": "irshad-audio"`

**`package-lock.json`** (2 правки):
- Строка 2 и 8: `"name": "salaf-audio"` → `"name": "irshad-audio"`

---

## Step 2: Замена localStorage ключей sa_ → ir_

**`src/App.jsx`** — все вызовы `localStorage`:

| Строка | Было | Стало |
|--------|------|-------|
| 14 | `sa_vol` | `ir_vol` |
| 15 | `sa_spd` | `ir_spd` |
| 19 | `sa_fav` | `ir_fav` |
| 20 | `sa_hist` | `ir_hist` |
| 26 | `sa_vol` | `ir_vol` |
| 27 | `sa_spd` | `ir_spd` |
| 28 | `sa_fav` | `ir_fav` |
| 29 | `sa_hist` | `ir_hist` |

---

## Step 3: Создание SVG-логотипа

Создать файл `public/favicon.svg` — минималистичный SVG-логотип "IRSHAD" с исламской тематикой (лунный серп + текст).

---

## Step 4: Замена favicon в index.html

Убедиться что `index.html:8` ссылается на `/favicon.svg` (уже есть).

---

## Step 5: Обновление sidebar-логотипа в App.jsx

Заменить иконку ☽ на SVG-логотип в сайдбаре (строка 109). Использовать inline SVG или CSS-градиент с текстом "IRSHAD".

---

## Step 6: Демо-запуск

- Запустить `npm run dev` для проверки результата в браузере.
- Проверить: favicon, sidebar логотип, title, профиль.

---

## Верификация
1. `grep -ri "salaf" src/ index.html package.json` — не должно быть результатов (кроме package-lock.json до обновления)
2. Визуальная проверка в браузере: логотип, название, favicon
3. Проверка localStorage ключей в DevTools → Application → Local Storage

## Критические файлы
- `src/App.jsx` — основной компонент (362 строки)
- `index.html` — заголовок страницы
- `package.json` — имя пакета
- `public/favicon.svg` — новый favicon (создать)

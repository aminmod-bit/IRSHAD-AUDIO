# Plan: Push to GitHub + Deploy GitHub Pages

## Ситуация
- Репозиторий `aminmod554-del/IRSHAD-AUDIO` на GitHub — пустой (ветка не запушена)
- Локально есть коммит "НАЧАЛО IRSHAD" с полным кодом
- Workflow `build.yml` есть, но без деплоя

## Шаг 1: Обновить `.github/workflows/build.yml`

Добавитьpermissions, upload-pages-artifact и deploy-pages:

```yaml
name: Build and Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

## Шаг 2: Запушить код

```
git push -u origin main
```

## Шаг 3: Включить GitHub Pages (вручную)

После пуша:
1. Зайти в `https://github.com/aminmod554-del/IRSHAD-AUDIO/settings/pages`
2. Source →选择 **GitHub Actions**
3. Сайт будет доступен на `https://aminmod554-del.github.io/IRSHAD-AUDIO/`

## Критические файлы
- `.github/workflows/build.yml` — замена содержимого

## Верификация
1. После пуша → Actions запустятся → Build succeeds → Deploy succeeds
2. Перейти по URL `https://aminmod554-del.github.io/IRSHAD-AUDIO/`
3. Проверить: favicon, логотип "IRSHAD", плеер работает
